# AI Architecture Contract

> Purpose: canonical repository-specific architecture contract. Replace this scaffold with evidence from the target repository and keep it as target-owned content after creation.

## Where Adjacent Content Belongs

- Use `docs/02-repository-context.md` for stable local operating context, glossary, ownership notes, and non-policy interpretation aids.
- Use `docs/03-ai-runtime-operating-model.md` for the source-managed runtime consumption model shared from the standards baseline.
- Use `AGENTS.md`, `.github/copilot-instructions.md`, or scoped `.github/instructions/*.instructions.md` for binding instruction policy.
- Use `LESSONS_LEARNED.md` for durable lessons that still need codification.

## Repository

Describe the repository's primary responsibility, consumers, and main inputs or outputs.

## Purpose

Describe what system this repository owns. If it is a library, infrastructure repo, service, documentation repo, or governance repo, say that directly.

## System Boundaries

In scope:

- `<add target-specific boundaries>`

Out of scope:

- `<add target-specific exclusions>`

## Main Components

| Component | Path | Responsibility |
| --- | --- | --- |
| `<component>` | `<path>` | `<responsibility>` |

## Architecture Flow

```text
<replace with a concise flow only when supported by repository evidence>
```

## Validation Surface

- `<add target-specific validation commands or checks>`

## Operational Notes

- Keep this file descriptive and evidence-based.
- Update this file when repository boundaries, component responsibilities, flows, or validation commands intentionally change.

## Risks And Open Questions

| Risk | Current evidence | Recommended handling |
| --- | --- | --- |
| `<risk>` | `<evidence>` | `<handling>` |

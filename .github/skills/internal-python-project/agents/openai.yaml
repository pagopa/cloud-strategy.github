interface:
  display_name: "internal-python-project"
  short_description: "Reusable Python packages, libraries, and services"
  default_prompt: "Use $internal-python-project for reusable imported Python code in packages, libraries, SDKs, services, framework handlers, thin CLI adapters over package code, and their tests."

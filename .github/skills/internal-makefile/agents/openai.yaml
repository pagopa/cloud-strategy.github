interface:
  display_name: "Internal Makefile"
  short_description: "Strict Makefile format handling"
  default_prompt: "Use $internal-makefile when editing or reviewing Make targets, prerequisites, recipe prefixes, .PHONY declarations, variables, or static format checks."

interface:
  display_name: "internal-review-code"
  short_description: "Repository wrapper for the imported code-review engine"
  default_prompt: "Use /internal-review-code as the repository wrapper and /addyosmani-code-review-and-quality as the sole review engine; conditionally load only the relevant GitHub Actions or composite-action contributor for changed surfaces; keep contributors bounded to observations and evidence; complete the review preflight, resolve target and source provenance, derive adversarial probes, run the ordered review sequence, return the report-only projection with one user action, use exactly MERGE READY, CHANGES REQUIRED, or REVIEW BLOCKED for the one public verdict, and stop with REVIEW BLOCKED when target, engine identity, or resolved source is unavailable."

interface:
  display_name: "internal-review-code"
  short_description: "Repository wrapper for the imported code-review engine"
  default_prompt: "Use /internal-review-code as the repository wrapper and /addyosmani-code-review-and-quality as the sole review engine; complete the review preflight, resolve the target and source provenance, derive adversarial probes from target evidence, run the ordered review sequence, return the report-only adaptive chat projection with one user action, use exactly MERGE READY, CHANGES REQUIRED, or REVIEW BLOCKED for the public verdict, and stop with REVIEW BLOCKED when the target, engine identity, or resolved source is unavailable."

interface:
  display_name: "Internal Review Code"
  short_description: "Repository wrapper for the imported code-review engine"
  default_prompt: "Use /internal-review-code as the repository wrapper, follow /addyosmani-code-review-and-quality as the complete review engine, return the result through the adaptive chat projection with one user action, and state that remediation requires a separate follow-up; use /awesome-copilot-security-review only if the user explicitly asks for security-specialist depth."

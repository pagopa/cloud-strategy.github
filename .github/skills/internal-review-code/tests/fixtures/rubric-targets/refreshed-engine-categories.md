# Refreshed engine categories

After the refresh, the engine labels each finding with one of these
categories:

- Critical
- Must fix
- Optional / Consider
- Nit
- FYI

`Required change` was renamed to `Must fix`; the wrapper mapping still lists
`Required change`.

interface:
  display_name: "Internal JSON"
  short_description: "JSON formatting and registry rules"
  default_prompt: "Use $internal-json when editing or reviewing repository-owned JSON registry, organization, data, or configuration files."

interface:
  display_name: "Local Sync External Resources"
  short_description: "Audit, plan, or apply declared external refreshes."
  default_prompt: "Use $local-agent-sync-external-resources to refresh only declared managed external resources through the staged sync CLI."

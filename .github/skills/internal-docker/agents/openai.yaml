interface:
  display_name: "internal-docker"
  short_description: "Dockerfile, Compose, and image hardening"
  default_prompt: "Use $internal-docker to create or modify Dockerfiles, Compose assets, image build settings, and container hardening rules."

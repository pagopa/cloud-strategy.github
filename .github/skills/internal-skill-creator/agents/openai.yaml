interface:
  display_name: "Internal Skill Creator"
  short_description: "First entrypoint for repo skill work"
  default_prompt: "Use $internal-skill-creator first to create or revise a repository-owned skill in .github/skills/. Establish the local boundary, keep the touched bundle self-contained, then load openai-skill-creator for the remaining generic bundle work; load anthropic-skill-creator only when the user explicitly requests Anthropic or Claude methodology."

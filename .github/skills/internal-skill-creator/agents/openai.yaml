interface:
  display_name: "Internal Skill Creator"
  short_description: "First entrypoint for repo skill work"
  default_prompt: "Use $internal-skill-creator first to create or revise a repository-owned skill in .github/skills/. Establish the local boundary, keep the touched bundle self-contained with bundle-local references and automation by default, then load the embedded OpenAI skill-creation workflow only for the remaining generic bundle work."

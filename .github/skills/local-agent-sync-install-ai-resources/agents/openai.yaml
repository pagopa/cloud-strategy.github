interface:
  display_name: "Home AI Resource Sync"
  short_description: "Plan, apply, audit, doctor, or bisync local AI home sync"
  default_prompt: "Use $local-agent-sync-install-ai-resources to run safe repo-to-home sync with compact output by default; require explicit approval for apply or bisync apply, and summarize compact results in chat Markdown for user decisions."

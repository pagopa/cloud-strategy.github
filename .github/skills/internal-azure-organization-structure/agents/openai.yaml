interface:
  display_name: "Internal Azure Organization Structure"
  short_description: "Azure tenant, landing-zone, and topology layout"
  default_prompt: "Use $internal-azure for an Azure organization-structure request, then apply $internal-azure-organization-structure to shape the tenant, management-group, subscription, landing-zone, or platform-topology structure."

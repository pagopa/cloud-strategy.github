interface:
  display_name: "local-sync-repos"
  short_description: "Mirror the managed instruction, hygiene, and Copilot approval baseline into consumer repos"
  default_prompt: "Use $local-sync-repos to plan or apply a consumer-repository baseline sync, including the workspace Copilot approval profile, from this standards repository."

policy:
  allow_implicit_invocation: false
interface:
  display_name: "internal-azure-governance"
  short_description: "Azure RBAC, Policy, and guardrail guidance"
  default_prompt: "Use /internal-azure for an Azure governance request, then apply $internal-azure-governance to review the RBAC, Policy, identity, or guardrail model."

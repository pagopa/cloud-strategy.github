interface:
  display_name: "Internal Terraform Router"
  short_description: "Wrap Anton for non-language Terraform work"
  default_prompt: "Use $internal-terraform as the stable Terraform/OpenTofu wrapper: delegate pure HCL to $internal-tf; otherwise route to Anton."

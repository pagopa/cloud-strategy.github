interface:
  display_name: "internal-terraform"
  short_description: "Route operational and mixed Terraform/OpenTofu work safely"
  default_prompt: "Use $internal-terraform as the stable Terraform/OpenTofu wrapper for operational, mixed, adoption, state, native test, CI, provider, recovery, and infrastructure-diagnosis requests. Route only positive language-only HCL or .tfvars.json work to /internal-tf; keep the wrapper primary for mixed or ambiguous requests, and hand off only an approved import to /internal-terraform-import with explicit scope and safety evidence. Load bundle-relative adoption or operational references conditionally. This is guidance, not runtime enforcement."

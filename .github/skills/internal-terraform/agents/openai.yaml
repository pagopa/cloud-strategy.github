interface:
  display_name: "Internal Terraform Router"
  short_description: "Wrap Anton for non-language Terraform work"
  default_prompt: "Use $internal-terraform as the stable Terraform/OpenTofu wrapper; delegate pure language directly to $internal-tf and use Anton for every other branch."

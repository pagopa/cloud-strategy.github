interface:
  display_name: "Codebase Improvement Gateway"
  short_description: "Analyze architecture and write a challenged plan"
  default_prompt: "Use $internal-gateway-codebase-improvement to analyze this codebase architecture, resolve every mandatory critical finding, and write an implementation plan without executing it."
policy:
  allow_implicit_invocation: false

interface:
  display_name: "Internal AWS"
  short_description: "AWS fallback for material routing uncertainty"
  default_prompt: "Use $internal-aws only when this AWS task has no clear specialist owner; clarify the lane and select the minimum AWS skill set."

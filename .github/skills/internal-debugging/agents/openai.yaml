interface:
  display_name: "internal-debugging"
  short_description: "Repository-owned root-cause failure diagnosis"
  default_prompt: "Use $internal-debugging as the repository-owned owner for root-cause diagnosis of bug, test, build, validator, sync, workflow, and catalog-drift failures; apply its self-contained baseline (red-capable loop, reproduction rate, regression check before fix, cleanup), use /mattpocock-diagnosing-bugs only as optional technique depth, and route measured slowness to /internal-performance-optimization."

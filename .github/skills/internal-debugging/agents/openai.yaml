interface:
  display_name: "internal-debugging"
  short_description: "Root-cause debugging for repository failures"
  default_prompt: "Use $internal-debugging to diagnose this failure from a reproducible feedback loop."

interface:
  display_name: "Internal GitHub"
  short_description: "GitHub fallback for material routing uncertainty"
  default_prompt: "Use $internal-github only when this GitHub task has no clear specialist owner; clarify the lane and select the minimum GitHub skill set."

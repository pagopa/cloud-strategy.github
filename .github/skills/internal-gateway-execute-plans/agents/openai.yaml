interface:
  display_name: "Execute Plans Internal Gateway"
  short_description: "Repository gateway for approved retained-plan execution"
  default_prompt: "Use /internal-gateway-execute-plans for approved retained-plan execution or resume under tmp/superpowers/plans/. Continue through safe, evidence-producing recovery and use closeout-check as the deterministic status decision owner. Delegate core execution to /superpowers-executing-plans, apply local TDD and verification gates, preserve routing and plan binding, write one legal status sibling, and never mutate Git."

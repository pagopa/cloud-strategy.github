interface:
  display_name: "Internal Graphify"
  short_description: "Use the local Graphify knowledge graph for this repository"
  default_prompt: "Use $internal-graphify when the repository exposes its activation gate markers and the question needs graph-style structure, communities, paths, or affected files. Verify graph-derived claims against real source files before finalizing the answer."

interface:
  display_name: "Internal Bash"
  short_description: "Bash safety and lightweight routing"
  default_prompt: "Use $internal-bash for embedded shell, Bash snippets, or non-operator Bash helpers that need lightweight safety, quoting, parser, or validation guidance before standalone script design."

interface:
  display_name: "Critical Master Internal Gateway"
  short_description: "Critical challenge for plans and decisions"
  default_prompt: "Use /internal-gateway-critical-master to run the full-scope challenge. Complete the three internal phases and emit exactly one validated internal-gateway-critical/full-analysis-v1 JSON packet with the current source, target path, target revision, every material finding, residual risks, and diagnostics. Do not emit a card, Markdown, prose, or any output outside the JSON packet."

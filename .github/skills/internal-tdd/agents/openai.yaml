interface:
  display_name: "internal-tdd"
  short_description: "Risk-driven TDD routing for code changes"
  default_prompt: "Use $internal-tdd for coding changes with executable or evaluable behavior: select exactly one test posture, choose the nearest native boundary, hand mandatory-test-first work to /superpowers-test-driven-development, delegate only through /internal-subagent-contract after the posture is locked, and report the posture record with observed evidence before any completion claim."

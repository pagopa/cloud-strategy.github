interface:
  display_name: "internal-subagent-contract"
  short_description: "Validate bounded caller-owned subagent briefs and results"
  default_prompt: "Use $internal-subagent-contract for one bounded, caller-owned brief/result pair; keep semantic worker fields separate from deterministic adapter data, validate the adapter-composed WorkerResult v1 and caller-owned VerificationReceipt v1, and never route, retry, or decide acceptance."

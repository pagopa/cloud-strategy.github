interface:
  display_name: "Internal Java"
  short_description: "Java source and build metadata"
  default_prompt: "Use $internal-java when Java language or source correctness, or generic Maven/Gradle build metadata, is the primary concern."

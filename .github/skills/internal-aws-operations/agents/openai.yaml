policy:
  allow_implicit_invocation: false
interface:
  display_name: "internal-aws-operations"
  short_description: "AWS monitoring, rollout, and recovery evidence"
  default_prompt: "Use /internal-aws to route this AWS request, then apply $internal-aws-operations for monitoring, logging, rollout validation, backup and restore proof, DR evidence, or audit evidence."

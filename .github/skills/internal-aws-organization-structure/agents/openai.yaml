policy:
  allow_implicit_invocation: false
interface:
  display_name: "internal-aws-organization-structure"
  short_description: "AWS account, OU, and control-plane layout"
  default_prompt: "Use /internal-aws to route this AWS request, then apply $internal-aws-organization-structure for Organizations, accounts, OUs, delegated administrators, StackSets topology, or platform network placement."

interface:
  display_name: "internal-yaml"
  short_description: "Strict YAML format handling"
  default_prompt: "Use $internal-yaml when editing or reviewing YAML syntax, encoding, duplicate keys, parser safety, or format-owner routing."

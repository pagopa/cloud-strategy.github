interface:
  display_name: Internal Gateway Review
  short_description: Defect-first review before fixes.
  default_prompt: "Use $internal-gateway-review to inspect the concrete review target, consolidate findings, counter-check the verdict, and stop before applying fixes."

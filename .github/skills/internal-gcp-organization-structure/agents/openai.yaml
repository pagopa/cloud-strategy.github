policy:
  allow_implicit_invocation: false
interface:
  display_name: "internal-gcp-organization-structure"
  short_description: "GCP org, project, and topology layout"
  default_prompt: "Use $internal-gcp-organization-structure when /internal-gcp selects organization structure for GCP placement, hierarchy, billing ownership, project segmentation, or Shared VPC topology."

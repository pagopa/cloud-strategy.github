interface:
  display_name: "Internal Azure"
  short_description: "Azure fallback for material routing uncertainty"
  default_prompt: "Use $internal-azure only when this Azure task has no clear specialist owner; clarify the lane and select the minimum Azure skill set."

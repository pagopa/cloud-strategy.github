interface:
  display_name: "Internal Python"
  short_description: "Python baseline and ownership routing"
  default_prompt: "Use $internal-python when Python work needs a lightweight shared baseline or its primary contract is still unclear."

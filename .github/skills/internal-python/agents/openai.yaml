interface:
  display_name: "internal-python"
  short_description: "Cross-cutting Python baseline"
  default_prompt: "Use $internal-python when Python work has no single primary contract, such as shared modules changed with independently executed scripts, cross-cutting concerns, or unclear ownership; it routes clear package, script, or PR-review work to its owner."

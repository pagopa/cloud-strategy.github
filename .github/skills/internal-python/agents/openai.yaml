interface:
  display_name: "internal-python"
  short_description: "Cross-cutting Python baseline"
  default_prompt: "Use $internal-python when Python work is cross-cutting, spans imported and direct-execution boundaries, or has unclear ownership."

interface:
  display_name: "internal-python-script"
  short_description: "Direct-execution Python tooling"
  default_prompt: "Use $internal-python-script for directly executed Python: standalone scripts and CLIs, automation or CI entrypoints, standalone data jobs, small operator toolkits, and their tests."

interface:
  display_name: "internal-performance-optimization"
  short_description: "Profiling and latency or throughput work"
  default_prompt: "Use $internal-performance-optimization when performance is the primary problem: profile the slowness first, then reduce latency, improve throughput, or prevent regressions across frontend, backend, and database layers."

interface:
  display_name: "Internal Gateway Simple"
  short_description: "Fast path for concrete skill-first tasks"
  default_prompt: "Use $internal-gateway-simple for a concrete task that needs quick support-skill discovery, focused execution, and validation without a retained plan."

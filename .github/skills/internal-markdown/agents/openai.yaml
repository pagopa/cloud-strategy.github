interface:
  display_name: "Internal Markdown"
  short_description: "Strict Markdown format handling"
  default_prompt: "Use $internal-markdown when editing or reviewing Markdown structure, fences, references, paths, links, or dialect-aware format checks."

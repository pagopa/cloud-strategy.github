interface:
  display_name: "internal-gcp"
  short_description: "Official and default GCP entry point"
  default_prompt: "Use $internal-gcp as the official and default entry point for any Google Cloud request; select and invoke the specialist that owns the primary deliverable."

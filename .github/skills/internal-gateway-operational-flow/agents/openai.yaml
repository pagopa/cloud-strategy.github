interface:
  display_name: "Internal Gateway Operational Flow"
  short_description: "Staged define-to-review workflow with one active phase at a time"
  default_prompt: "Use $internal-gateway-operational-flow to run the visible staged workflow. Choose one active phase from the state machine. Follow gate-0-protocol.md for Gate 0 depth. Stay in define until the user explicitly requests the next phase. Do not restart Gate 0 for approved retained plans."

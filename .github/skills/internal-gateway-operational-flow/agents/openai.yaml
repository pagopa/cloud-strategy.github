interface:
  display_name: "Internal Gateway Operational Flow"
  short_description: "Define-to-review workflow with grill-me Gate 0"
  default_prompt: "Use $internal-gateway-operational-flow with this request or retained plan folder to choose the visible define-first, full-cycle, plan-only, apply-plan, review, or explicit phase workflow. Run the minimum evidence pass and follow gate-0-protocol.md for every non-execute entrypoint. For define-first or brainstorming, stay in define until the user explicitly requests planning."
